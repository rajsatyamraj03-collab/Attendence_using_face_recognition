# Student Attendance System - Requirements
# Install with: pip install -r requirements.txt

flask>=3.0.0
flask-cors>=4.0.0
numpy>=1.24.0
Pillow>=10.0.0

# Face Recognition (requires cmake + dlib)
# On Ubuntu/Debian: sudo apt-get install cmake libboost-all-dev
# Then: pip install dlib face-recognition
# 
# If you can't install face_recognition, the system runs in demo mode.
face-recognition>=1.3.0
